import { Stack, Typography } from '@mui/material';

export default function loading() {
    return (
        <Stack sx={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Typography>maus</Typography>
            <Typography>maus</Typography>
            <Typography>maus</Typography>
        </Stack>
    );
}
